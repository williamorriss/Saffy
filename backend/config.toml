[server]
port = 8000
origin = "https://localhost:8000" # Make sure port matches origin
host = "127.0.0.1"
key_location = "key.pem" # Seeded to tsl
cert_location = "cert.pem"

[cas]
origin = "https://auth.bath.ac.uk"

[frontend]
dist = "/Users/bill/IdeaProjects/knowle/frontend/dist"

[database]
max_connections = 5